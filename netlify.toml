# Instrucciones para Netlify: la página está en la carpeta "frontend"
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  # Dirección del backend publicado en Render
  VITE_API_URL = "https://panaderia-cainzo-backend.onrender.com"

# La app tiene varias pantallas (/login, /ventas, ...): siempre se sirve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
